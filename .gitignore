# File di lavoro — non pubblicare
scraps/
screenshots/
uploads/
dist/

# Schema del database — script SQL da eseguire su Supabase (nel repo, così sono
# accessibili a tutto lo staff). Eventuali file di lavoro locali in db/ restano esclusi.
db/*
!db/*.sql

# Sistema
.DS_Store
